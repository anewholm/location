# ===================================
#  Form Behavior Config
# ===================================

# Record name
name: acorn.location::lang.models.area.label

# Model Form Field configuration
form: $/acorn/location/models/area/fields.yaml

# Model Class name
modelClass: Acorn\Location\Models\Area

# Default redirect location
defaultRedirect: acorn/locations/areas

# Create page
create:
    title: backend::lang.form.create_title
    redirect: acorn/locations/areas/update/:id
    redirectClose: acorn/locations/areas

# Update page
update:
    title: backend::lang.form.update_title
    redirect: acorn/locations/areas
    redirectClose: acorn/locations/areas

# Preview page
preview:
    title: backend::lang.form.preview_title
