# ===================================
#  Form Behavior Config
# ===================================

# Record name
name: acorn.location::lang.models.address.label

# Model Form Field configuration
form: $/acorn/location/models/address/fields.yaml

# Model Class name
modelClass: Acorn\Location\Models\Address

# Default redirect location
defaultRedirect: acorn/locations/addresses

# Create page
create:
    title: backend::lang.form.create_title
    redirect: acorn/locations/addresses/update/:id
    redirectClose: acorn/locations/addresses

# Update page
update:
    title: backend::lang.form.update_title
    redirect: acorn/locations/addresses
    redirectClose: acorn/locations/addresses

# Preview page
preview:
    title: backend::lang.form.preview_title
